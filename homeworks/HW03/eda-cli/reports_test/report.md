# EDA Report

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Параметры отчета

- Top-K Categories: 5
- Min Missing Share Threshold: 5%
- Max Histograms: 6

## Качество данных (эвристики)

**Общая оценка качества: 0.54** (0.0 — плохо, 1.0 — отлично)

### Базовые проверки

- Слишком мало строк (<100): **True**
- Слишком много колонок (>100): **False**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком много пропусков (>50%): **False**

### Обнаруженные проблемы

✓ Константные колонки не найдены

⚠️ **Найдены дубликаты в ID-колонках:**
  - `user_id` (содержит повторяющиеся значения)

_Это может указывать на проблемы с целостностью данных._

## Колонки

Подробная информация в файле `summary.csv`.

## Пропуски

### Колонки с >5% пропусков:

|                          |   missing_count |   missing_share |
|:-------------------------|----------------:|----------------:|
| city                     |               2 |       0.0555556 |
| avg_session_duration_min |               2 |       0.0555556 |

Подробнее см. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
